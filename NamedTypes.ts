// Named Types

type statusTypes = "started" | "success" | "failed";
type ToggleSwitch="on"|"off";

let currentStatus: statusTypes = "started";

let res = "pending";

if (res === "pending") {
    currentStatus = "success";
    console.log(currentStatus);
    
}


let toggle:ToggleSwitch="off";

toggle="on"

const Tswitch="on";

if(Tswitch=="on"){
   let torch= toggle="off"
   console.log(torch);
   
}